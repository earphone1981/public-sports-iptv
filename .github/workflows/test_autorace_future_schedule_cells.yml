name: Test AutoRace Future Schedule Cells

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Test AutoRace cell-based schedule
        run: python test_autorace_future_schedule_cells.py
